// providers/waec.js  (aggregator version)
export async function verify({ examNumber, examYear, examType, epin }) {
  // call aggregator WAEC endpoint
  // return { verdict: 'verified'|'failed', normalizedFields, raw }
}
